<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{{ this.title}}} {{this.user.userName}}</h2>
  </div>
  <div class="col-md-6">
    <h3>{{this.content}}</h3>
    {{#each Comments }}
{{> comment-details }}
{{/each}}
  </div>
</div>